---
import Base from '../layouts/Base.astro';
import projectsData from '../data/projects.json';

const iconMap: Record<string, string> = {
  GARDEN:    'Icons2-01.svg',
  SPACESHIP: 'Icons2-04.svg',
  MYSTERIES: 'Icons2-03.svg',
  ASSEMBLY:  'Icons2-02.svg',
};
const orientationLabels: Record<string, string> = {
  GARDEN:    'The Garden',
  SPACESHIP: 'Spaceship Earth',
  MYSTERIES: 'The Mysteries',
  ASSEMBLY:  'The Assemblies',
};
const orientations = ['GARDEN','SPACESHIP','MYSTERIES','ASSEMBLY'] as const;

// Status display config
const STATUS_COLOR: Record<string, string> = {
  active:    '#2e7d32',
  completed: '#64748b',
  planned:   '#b45309',
  paused:    '#1d4ed8',
  archived:  '#9ca3af',
};
const STATUS_LABEL: Record<string, string> = {
  active:    'Active',
  completed: 'Completed',
  planned:   'Planned',
  paused:    'Paused',
  archived:  'Archived',
};

// Status pills for filter — count from data
const STATUS_ORDER = ['active','planned','completed','paused','archived'] as const;
const statusCounts: Record<string, number> = {};
STATUS_ORDER.forEach(s => { statusCounts[s] = 0; });
(projectsData as any[]).forEach((p: any) => {
  const s = (p.status ?? 'active').toLowerCase();
  statusCounts[s] = (statusCounts[s] ?? 0) + 1;
});

const total = projectsData.length;

function getFavicon(website: string): string {
  if (!website) return '';
  try {
    const url = new URL(website);
    return `https://icons.duckduckgo.com/ip3/${url.hostname}.ico`;
  } catch { return ''; }
}
---

<Base title="Projects — The Orbital">
  <div class="projects-page">

    <div class="projects-header">
      <h1 class="projects-title">Projects</h1>
      <p class="projects-desc">Specific initiatives and experiments at the frontier of planetary governance.</p>
    </div>

    <!-- TODO(olle): editorial intro
         Add a short paragraph here (2–4 sentences) contextualising the
         projects index — what counts as a "project" vs an "actor", how
         to read the status labels, and what the index covers.
         Remove this comment and uncomment the block below when ready.

    <div class="editorial-intro">
      <p>…</p>
    </div>
    -->

    <!-- Filter bar -->
    <div class="filter-bar" id="filter-bar">
      {orientations.map(key => (
        <button class="filter-btn active" data-orientation={key} aria-pressed="true">
          <img src={`/assets/${iconMap[key]}`} alt={orientationLabels[key]} class="filter-icon" />
          <span class="filter-label">{orientationLabels[key]}</span>
        </button>
      ))}
    </div>

    <div class="secondary-filters" id="secondary-filters">
      <div class="filter-row">
        <span class="filter-row-label">Status</span>
        <div class="filter-row-pills">
          {STATUS_ORDER.filter(s => statusCounts[s] > 0).map(s => (
            <button
              class="pill-btn active status-btn"
              data-status={s}
              aria-pressed="true"
            >
              <span class="status-dot" style={`background:${STATUS_COLOR[s]}`}></span>
              {STATUS_LABEL[s]}
              <span class="pill-count">({statusCounts[s]})</span>
            </button>
          ))}
        </div>
      </div>
    </div>

    <!-- Count -->
    <div class="count-row">
      <span class="project-count" id="project-count">{total} projects</span>
    </div>

    <!-- Grid -->
    <div class="projects-grid" id="projects-grid">
      {(projectsData as any[]).map((project: any) => {
        const status = (project.status ?? 'active').toLowerCase();
        const accentColor = STATUS_COLOR[status] ?? '#64748b';
        const hasImage = !!(project.image);
        return (
          <a
            href={project.website || '#'}
            target={project.website ? '_blank' : undefined}
            rel={project.website ? 'noopener' : undefined}
            class="project-card"
            data-orientations={project.orientations.join(',')}
            data-status={status}
            style={`--accent:${accentColor}`}
          >
            {hasImage && (
              <div class="card-image" style={`background-image:url('${project.image}')`} aria-hidden="true"></div>
            )}
            <div class="card-body">
              <div class="card-top">
                <span class="card-status">
                  <span class="status-dot" style={`background:${accentColor}`}></span>
                  {STATUS_LABEL[status] ?? status}
                </span>
              </div>
              <h3 class="card-name">
                {getFavicon(project.website) && (
                  <img src={getFavicon(project.website)} alt="" class="card-favicon" referrerpolicy="no-referrer" onerror="this.style.display='none'" loading="lazy" />
                )}
                {project.name}
              </h3>
              <p class="card-desc">{project.description}</p>
              {project.geography && (
                <p class="card-geo">
                  <svg class="geo-icon" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
                    <path d="M8 1.5C5.5 1.5 3.5 3.5 3.5 6c0 3.5 4.5 8.5 4.5 8.5s4.5-5 4.5-8.5c0-2.5-2-4.5-4.5-4.5z"/>
                    <circle cx="8" cy="6" r="1.25"/>
                  </svg>
                  {project.geography}
                </p>
              )}
            </div>
            <div class="card-footer">
              <div class="card-tags">
                {(project.tags ?? []).slice(0, 3).map((tag: string) => (
                  <span class="card-tag">{tag.replace(/-/g, '\u00a0')}</span>
                ))}
              </div>
              <div class="card-icons">
                {(project.orientations ?? []).map((o: string) => (
                  <img src={`/assets/${iconMap[o]}`} alt={orientationLabels[o]} class="card-orient-icon" title={orientationLabels[o]} />
                ))}
              </div>
            </div>
          </a>
        );
      })}
    </div>

    <p class="no-results" id="no-results" style="display:none">No projects match your current filters.</p>
  </div>
</Base>

<style>
/* ── Page ── */
.projects-page {
  max-width: 1300px;
  margin: 0 auto;
  padding: 2rem 2rem 4rem;
}
.projects-header { padding-bottom: 1rem; }
.projects-title {
  font-family: 'Playfair Display', Georgia, serif;
  font-size: 2rem;
  font-weight: 400;
  margin-bottom: .5rem;
}
.projects-desc { font-size: .9375rem; color: var(--text-2); }

/* ── Editorial intro ── */
.editorial-intro {
  font-size: .9375rem;
  color: var(--text-2);
  line-height: 1.7;
  margin: .5rem 0 1.5rem;
  max-width: 680px;
}

/* ── Filter bar (orientations) ── */
.filter-bar {
  display: flex;
  justify-content: center;
  gap: 2.5rem;
  flex-wrap: wrap;
  align-items: flex-start;
  padding: 1rem 1.5rem;
  background: var(--filter-bg);
  border-radius: 10px 10px 0 0;
  border-bottom: 1px solid var(--border);
}
.filter-btn {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: .35rem;
  background: none;
  border: none;
  cursor: pointer;
  padding: 0;
  opacity: 1;
  transition: transform .15s, opacity .15s;
}
.filter-btn:hover { transform: scale(1.08); }
.filter-btn:not(.active) { opacity: .28; }
.filter-icon { width: 32px; height: 32px; object-fit: contain; }
.filter-label { font-size: .6875rem; color: var(--text-3); white-space: nowrap; letter-spacing: .04em; }

/* ── Secondary filters ── */
.secondary-filters {
  display: flex;
  flex-direction: column;
  gap: .375rem;
  padding: .75rem 1.25rem;
  background: var(--filter-bg);
  border-radius: 0 0 10px 10px;
  margin-bottom: 1.5rem;
}
.filter-row { display: flex; align-items: center; gap: .5rem; flex-wrap: wrap; }
.filter-row-label {
  font-family: var(--font-sans);
  font-size: .625rem;
  font-weight: 700;
  letter-spacing: .1em;
  text-transform: uppercase;
  color: var(--text-3);
  min-width: 44px;
  flex-shrink: 0;
}
.filter-row-pills { display: flex; flex-wrap: wrap; gap: .3rem; }
.pill-btn {
  display: flex;
  align-items: center;
  gap: .35rem;
  font-size: .6875rem;
  font-weight: 500;
  padding: .2rem .625rem;
  border: 1px solid var(--border);
  border-radius: 999px;
  background: var(--bg);
  color: var(--text-3);
  cursor: pointer;
  transition: background .12s, color .12s, border-color .12s;
  line-height: 1.5;
  white-space: nowrap;
}
.pill-btn:hover { border-color: var(--text-2); color: var(--text-2); }
.pill-btn.active {
  background: var(--text);
  color: var(--bg);
  border-color: var(--text);
}
.pill-btn.active:hover { opacity: .85; }
.pill-count { opacity: .65; font-size: .625rem; }
.pill-btn.active .pill-count { opacity: .8; }

/* ── Count row ── */
.count-row {
  display: flex;
  justify-content: flex-end;
  margin-bottom: 1.5rem;
}
.project-count { font-size: .8125rem; color: var(--text-3); }

/* ── Grid ── */
.projects-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 1.5rem;
}
@media (max-width: 1100px) { .projects-grid { grid-template-columns: repeat(3, 1fr); } }
@media (max-width: 760px)  { .projects-grid { grid-template-columns: repeat(2, 1fr); } }
@media (max-width: 480px)  { .projects-grid { grid-template-columns: 1fr; } }

/* ── Project card ── */
.project-card {
  position: relative;
  display: flex;
  flex-direction: column;
  border: 1px solid var(--border);
  border-top: 3px solid var(--accent, var(--border));
  border-radius: 0 0 10px 10px;
  background: var(--bg);
  text-decoration: none;
  color: inherit;
  overflow: hidden;
  transition: box-shadow .2s, transform .2s, border-color .2s;
}
.project-card:hover {
  box-shadow: 0 6px 20px rgba(0,0,0,.08);
  transform: translateY(-2px);
  border-top-color: var(--accent);
  text-decoration: none;
}
.project-card.hidden { display: none; }

/* ── Card image strip ── */
.card-image {
  width: 100%;
  height: 100px;
  background-size: cover;
  background-position: center;
  flex-shrink: 0;
}

/* ── Card body ── */
.card-body {
  padding: 1.125rem 1.25rem .75rem;
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: .5rem;
}
.card-top {
  display: flex;
  align-items: center;
  gap: .5rem;
}
.card-status {
  display: inline-flex;
  align-items: center;
  gap: .3rem;
  font-size: .625rem;
  font-weight: 700;
  letter-spacing: .08em;
  text-transform: uppercase;
  color: var(--text-3);
}
.status-dot {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  flex-shrink: 0;
}
.card-name {
  display: flex;
  align-items: center;
  gap: .5rem;
  font-family: 'Playfair Display', Georgia, serif;
  font-size: 1rem;
  font-weight: 400;
  line-height: 1.35;
  color: var(--text);
}
.card-favicon {
  width: 18px;
  height: 18px;
  object-fit: contain;
  border-radius: 3px;
  flex-shrink: 0;
  opacity: .75;
}
.card-desc {
  font-size: .8125rem;
  line-height: 1.65;
  color: var(--text-2);
  flex: 1;
}
.card-geo {
  display: flex;
  align-items: center;
  gap: .3rem;
  font-size: .75rem;
  color: var(--text-3);
  margin-top: .125rem;
}
.geo-icon {
  width: 11px;
  height: 11px;
  flex-shrink: 0;
  opacity: .6;
}

/* ── Card footer ── */
.card-footer {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: .5rem;
  padding: .625rem 1.25rem .875rem;
  border-top: 1px solid var(--border);
  background: color-mix(in srgb, var(--accent) 3%, var(--bg));
}
.card-tags {
  display: flex;
  flex-wrap: wrap;
  gap: .25rem;
  flex: 1;
}
.card-tag {
  font-size: .625rem;
  color: var(--text-3);
  background: var(--filter-bg);
  border: 1px solid var(--border);
  border-radius: 3px;
  padding: .1rem .35rem;
  white-space: nowrap;
}
.card-icons { display: flex; gap: .25rem; flex-shrink: 0; }
.card-orient-icon { width: 18px; height: 18px; object-fit: contain; opacity: .75; }

/* ── No results ── */
.no-results {
  text-align: center;
  padding: 3rem 0;
  font-size: .9375rem;
  color: var(--text-3);
}
</style>

<script>
// ── Filter state ────────────────────────────────────────────────────────────
const ORIENTATIONS = ['GARDEN','SPACESHIP','MYSTERIES','ASSEMBLY'];
let activeOrientations = new Set(ORIENTATIONS);
let activeStatuses     = new Set<string>();

// Init statuses from rendered pills
document.querySelectorAll<HTMLElement>('.status-btn').forEach(b => {
  activeStatuses.add(b.dataset.status!);
});

// ── Helpers ─────────────────────────────────────────────────────────────────
function pushURL() {
  const params = new URLSearchParams();
  const missingO = ORIENTATIONS.filter(o => !activeOrientations.has(o));
  if (missingO.length) params.set('o', [...activeOrientations].join(','));
  const allStatuses = [...document.querySelectorAll<HTMLElement>('.status-btn')].map(b => b.dataset.status!);
  const missingS = allStatuses.filter(s => !activeStatuses.has(s));
  if (missingS.length) params.set('st', [...activeStatuses].join(','));
  const qs = params.toString();
  history.replaceState(null, '', qs ? '?' + qs : location.pathname);
}

function applyFilters() {
  const cards = document.querySelectorAll<HTMLElement>('.project-card');
  let visible = 0;
  cards.forEach(card => {
    const orientations = (card.dataset.orientations || '').split(',').filter(Boolean);
    const status       = card.dataset.status ?? '';
    const passO = orientations.length === 0 || orientations.some(o => activeOrientations.has(o));
    const passSt = activeStatuses.has(status);
    const show = passO && passSt;
    card.classList.toggle('hidden', !show);
    if (show) visible++;
  });
  const countEl = document.getElementById('project-count');
  if (countEl) countEl.textContent = visible + ' project' + (visible !== 1 ? 's' : '');
  const noResults = document.getElementById('no-results');
  if (noResults) noResults.style.display = visible === 0 ? '' : 'none';
}

function syncButtons() {
  document.querySelectorAll<HTMLElement>('.filter-btn[data-orientation]').forEach(btn => {
    const active = activeOrientations.has(btn.dataset.orientation!);
    btn.classList.toggle('active', active);
    btn.setAttribute('aria-pressed', String(active));
  });
  document.querySelectorAll<HTMLElement>('.status-btn').forEach(btn => {
    const active = activeStatuses.has(btn.dataset.status!);
    btn.classList.toggle('active', active);
    btn.setAttribute('aria-pressed', String(active));
  });
}

// ── Init from URL ────────────────────────────────────────────────────────────
(function initFromURL() {
  const params = new URLSearchParams(location.search);
  const o  = params.get('o');
  const st = params.get('st');
  if (o)  activeOrientations = new Set(o.split(',').filter(x => ORIENTATIONS.includes(x)));
  if (st) {
    const allStatuses = [...document.querySelectorAll<HTMLElement>('.status-btn')].map(b => b.dataset.status!);
    activeStatuses = new Set(st.split(',').filter(x => allStatuses.includes(x)));
  }
  syncButtons();
  applyFilters();
})();

// ── Orientation buttons ──────────────────────────────────────────────────────
document.getElementById('filter-bar')!.addEventListener('click', e => {
  const btn = (e.target as Element).closest<HTMLElement>('.filter-btn[data-orientation]');
  if (!btn) return;
  const key = btn.dataset.orientation!;
  if (activeOrientations.has(key)) {
    if (activeOrientations.size === 1) { activeOrientations = new Set(ORIENTATIONS); }
    else activeOrientations.delete(key);
  } else {
    activeOrientations.add(key);
  }
  syncButtons();
  applyFilters();
  pushURL();
});

// ── Status buttons ────────────────────────────────────────────────────────────
document.getElementById('secondary-filters')!.addEventListener('click', e => {
  const btn = (e.target as Element).closest<HTMLElement>('.status-btn');
  if (!btn) return;
  const key = btn.dataset.status!;
  if (activeStatuses.has(key)) {
    if (activeStatuses.size === 1) {
      // Re-select all
      document.querySelectorAll<HTMLElement>('.status-btn').forEach(b => activeStatuses.add(b.dataset.status!));
    } else {
      activeStatuses.delete(key);
    }
  } else {
    activeStatuses.add(key);
  }
  syncButtons();
  applyFilters();
  pushURL();
});
</script>
